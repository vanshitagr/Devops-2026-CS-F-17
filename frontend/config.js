// App-wide constants that aren't category/location data (which
// live in constants.js) — kept separate so config vs. domain data
// don't get mixed in one file.
export const APP_NAME = "GigBoard";
export const APP_TAGLINE = "Campus micro-gigs for SKIT Jagatpura";
export const MAX_BID_MESSAGE_LENGTH = 500;